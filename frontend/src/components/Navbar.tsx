import React from 'react';
import Logout from "./auth/Logout";

const Navbar = () => {
    return (
        <div>
            <Logout />
            <b>Logout</b>
        </div>
    );
};

export default Navbar;
